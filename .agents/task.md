# WMS Navigation & Page Merging Tasks

## 1. Frontend Architecture (`nextjs-frontend-architect`)
- [/] Merge WMS Dashboard (`/modules/wms`) and Items Catalog (`/modules/wms/items`) into a single consolidated view on `/modules/wms`.
- [ ] Update `src/lib/config/nav.ts` to reflect the unified route and remove redundant child items.
- [ ] Ensure unified EPS visual style, filter toolbar, search, KPI grid, and modal dialogs.

## 2. QA & Verification (`qa-code-reviewer`)
- [ ] Run `npx tsc --noEmit` type checking.
- [ ] Run `npx next build` production build verification.


